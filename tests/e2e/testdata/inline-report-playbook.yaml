apiVersion: mission-control.flanksource.com/v1
kind: Playbook
metadata:
  name: inline-report-test
  namespace: default
spec:
  title: Inline Report Test
  configs:
    - types:
        - Kubernetes::Pod
  actions:
    - name: generate-inline-report
      report:
        configs:
          types:
            - Kubernetes::Pod
        format: json

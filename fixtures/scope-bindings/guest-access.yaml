apiVersion: mission-control.flanksource.com/v1
kind: ScopeBinding
metadata:
  name: guest-access
  namespace: mc
spec:
  description: Grant guest users access to production configs
  subjects:
    persons:
      - aditya+guest@flanksource.com
  scopes:
    - prod-configs

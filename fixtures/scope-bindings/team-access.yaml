apiVersion: mission-control.flanksource.com/v1
kind: ScopeBinding
metadata:
  name: team-access
  namespace: mc
spec:
  description: Grant platform team access to staging components
  subjects:
    teams:
      - platform-team
    persons:
      - aditya@flanksource.com
  scopes:
    - staging-components

---
apiVersion: mission-control.flanksource.com/v1
kind: AccessScope
metadata:
  name: guest-access-all-echo-playbooks
  namespace: mc
spec:
  description: Allow guest user read access to all echo playbooks in guest1-ns namespace
  subject:
    person: aditya+guest@flanksource.com
  resources: ["playbook"]
  scopes:
    - names:
        - echo-secret-parameter
        - loki-logs

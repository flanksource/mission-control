apiVersion: mission-control.flanksource.com/v1
kind: Notification
metadata:
  name: http-check-passed
spec:
  events:
    - check.passed
  filter: check.type == 'http'
  watchdog:
    schedule: "@every 2h"
  to:
    team: backend

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incident-commander.fullname" . }}-test-connection"
  labels:
    {{- include "incident-commander.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incident-commander.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-commander.fullname" . }}
  labels:
    {{- include "incident-commander.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incident-commander.selectorLabels" . | nindent 4 }}

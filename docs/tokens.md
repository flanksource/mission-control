# Tokens in Mission Control

At a high level Mission Control has two credential families:

1. **Access tokens** — long-lived, opaque, DB-backed, revocable credentials.
2. **Signed tokens** — short-lived JWTs minted for a specific subsystem or protocol.

Access tokens can be used to authenticate a request. After authentication, Mission Control may mint an internal signed token for downstream use.

## Mission Control access tokens

Long-lived access tokens are opaque Mission Control credentials generated by `db.CreateAccessToken(...)` and stored hashed in the `access_tokens` table.

They are different from the short-lived signed JWTs described below: access tokens are revocable database-backed credentials intended for non-browser clients and delegated identities.

### Token format

The returned token currently has this format:

```text
password.salt.timeCost.memoryCost.parallelism
```

Only the Argon2id hash is stored in the database. The raw token is shown once to the caller.

### Issuance flows

| Flow                 | Where                                          | Subject                               | Lifetime                                                                 | Use case                                                                             |
| -------------------- | ---------------------------------------------- | ------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| Agent token          | `agent/agent.go`                               | Agent `Person`                        | 365d                                                                     | Primary intended long-lived credential for agents.                                   |
| Delegated user token | `auth/controllers.go` → `/auth/create_token`   | New `Person` with type `access_token` | Default 90d via `access_token.default_expiry`; request can supply expiry | Scoped/delegated token, mainly used by MCP clients that need restricted permissions. |
| CLI-created token    | `cmd/token.go`                                 | Existing user `Person`                | Default 4h unless `--expiry` supplied                                    | Admin/operator-created token from CLI.                                               |
| Kubeconfig token     | `echo/kube_config_download.go` → `/kubeconfig` | Current user `Person`                 | Currently non-expiring (`expiry=nil`)                                    | Embedded in kubeconfig as `username: token`, `password: <token>` for `/kubeproxy`.   |

### Delegated token model

`CreateAccessTokenForPerson(...)` exists to create a separate RBAC subject for a token.

For `/auth/create_token`:

1. A new `people` row is created with `type = access_token`.
2. An access token is created for that token-person.
3. `access_tokens.created_by` points back to the real user.
4. The token-person is grouped to the creator in RBAC.
5. Additional allow/deny permissions can be applied to the token-person.

This allows delegated MCP tokens to have a subset of the user's permissions, or token-specific permissions, without changing the user's own permissions.

### Authentication

Access tokens are accepted as:

```http
Authorization: Bearer <token>
```

or with basic auth:

```text
username: token
password: <token>
```

Validation happens through `auth.getAccessToken(...)`.

### Revocation

Access tokens are revocable by deleting their `access_tokens` row. For delegated user tokens, the associated `access_token` person is soft-deleted as well.

---

## Signed tokens

Signed tokens are short-lived JWTs minted for a specific Mission Control subsystem or protocol. They are created after another authentication step and are not durable, database-backed credentials.

They are different from long-lived access tokens, which are opaque, stored hashed in the `access_tokens` table, and explicitly revocable.

### Token types

| Token                          | Where generated                                                      |                                                        Lifetime | Audience / consumer                                          | Use case                                                                                                                                    |
| ------------------------------ | -------------------------------------------------------------------- | --------------------------------------------------------------: | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Internal PostgREST/session JWT | `auth.GetOrCreateJWTToken(...)` in `auth/tokens.go`                  |                                                      Cached 1h; | Mission Control/PostgREST/RLS                                | Injected after successful auth. Carries `role`, `id`, and RLS claims used by PostgREST and DB row-level security.                           |
| Basic login cookie JWT         | `auth.BasicLogin(...)` in `auth/basic.go`                            | Browser session cookie; same JWT format as internal session JWT | Mission Control browser auth                                 | Stores the internal JWT in an HTTP-only cookie for basic-auth UI sessions.                                                                  |
| Plugin invocation JWT          | `auth.MintPluginInvocationToken(...)` in `auth/plugin_invocation.go` |                Default 5m; configurable with `--plugin-jwt-ttl` | Plugin host/runtime                                          | Short-lived proof that Mission Control authorized a plugin operation. Sent via `X-Flanksource-Plugin-Invocation` or gRPC metadata.          |
| Embedded OIDC access/ID JWT    | Embedded OIDC provider in `auth/oidc`                                |                                                              1h | MCP/native OAuth clients and Mission Control auth middleware | Standards-compliant OAuth/OIDC token for MCP/native clients. Mission Control validates it and converts it into normal request auth context. |

### Not counted here

The embedded OIDC flow also creates short-lived protocol artifacts such as authorization codes and transaction cookies. Those are intermediaries for completing OAuth login and are not Mission Control access credentials by themselves.

### Signing keys and crypto material

Signing keys and secrets are not tokens, but they are critical authentication material for signed tokens.

Mission Control owns or generates **four** signing/crypto materials for the tokens above.

| Key / secret         | Where                                                                            | Default storage                                                                                         | Used for                                                                                                       | Notes                                                                                                     |
| -------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| PostgREST JWT secret | `cmd/jwt_secret.go`; `dutyAPI.DefaultConfig.Postgrest.JWTSecret`                 | Local dev: `.mission-control.jwt`; otherwise configured through Duty/PostgREST config                   | Signs internal PostgREST/session JWTs in `auth/tokens.go`; verifies basic login cookie JWTs in `auth/basic.go` | `ensureLocalJWTSecret()` creates a local secret only when no configured secret exists.                    |
| Plugin JWT secret    | `auth/plugin_invocation.go`; flag `--plugin-jwt-secret`; env `PLUGIN_JWT_SECRET` | If not configured: generated in memory at process start                                                 | Signs and verifies plugin invocation JWTs                                                                      | Non-persistent generated value means plugin invocation JWTs are invalidated on process restart.           |
| OIDC RSA signing key | `auth/oidc/provider.go`; flag `--oidc-signing-key`                               | Default file: `oidc-signing-key.pem`                                                                    | Signs embedded OIDC access/ID JWTs                                                                             | Generated if missing. The public key is upserted into `oidc_public_keys` and served via the OIDC key set. |
| OIDC crypto key      | `auth/oidc/provider.go`                                                          | Default file: `oidc-crypto-key.key`; if `--oidc-signing-key=/path/key.pem`, then `/path/key.pem.crypto` | Zitadel OIDC provider crypto key; also used as the HMAC key for OIDC transaction cookies                       | 32-byte secret generated if missing. Not a JWT signing key.                                               |

### Public verification material

| Material         | Where                                                       | Used for                    | Notes                                                                               |
| ---------------- | ----------------------------------------------------------- | --------------------------- | ----------------------------------------------------------------------------------- |
| OIDC public keys | `oidc_public_keys` table; derived from OIDC RSA signing key | Verifies embedded OIDC JWTs | Public half of the OIDC signing key. Safe to expose through JWKS/key-set endpoints. |
| Clerk JWKS URL   | `--clerk-jwks-url`                                          | Verifies Clerk session JWTs | External verification material; Mission Control does not generate these keys.       |
